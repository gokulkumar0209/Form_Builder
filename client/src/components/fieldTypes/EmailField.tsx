import React from "react";
import PropTypes from "prop-types";

interface EmailProps {}
const EmailField: React.FC<EmailProps> = ({}) => {
	return <div>EmailField</div>;
};

EmailField.propTypes = {};

export default EmailField;
